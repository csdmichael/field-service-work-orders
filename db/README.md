# Field Service Work Orders — Database

Forward-only migrations in `migrations/`, numbered and never edited once merged.
`schema.sql` is the consolidated current state, regenerated after each migration.
